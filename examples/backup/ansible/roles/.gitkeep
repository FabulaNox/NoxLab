# One directory per role (base-hardening/, docker-platform/, ...), each with
# the usual tasks/, templates/, handlers/, defaults/ layout. Kept minimal here;
# the real roles live in the private infra repo.
